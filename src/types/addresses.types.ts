export type ProvinceItem = {
  _id: string
  code: string
  name: string
}

export type DistrictItem = {
  id: string
  name: string
}

export type WardItem = {
  id: string
  name: string
  prefix: string
}
